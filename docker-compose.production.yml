version: '3'

volumes:
  pg_data_production:
  static_volume:
  media_volume:

services:
  db:
    env_file: .env
    image: postgres:13
    volumes:
      - pg_data_production:/var/lib/postgresql/data
  backend:
    env_file: .env
    image: labamoon/kittygram_backend
    volumes:
      - static_volume:/static
      - media_volume:/app/media
    depends_on:
      - db
  frontend:
    env_file: .env
    image: labamoon/kittygram_frontend
    command: cp -r /app/build/. /static/
    volumes:
      - static_volume:/static
  gateway:
    env_file: .env
    image: labamoon/kittygram_gateway
    ports:
      - 9000:80
    depends_on:
      - backend
    volumes:
      - static_volume:/static
      - media_volume:/app/media
